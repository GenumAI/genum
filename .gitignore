node_modules
.DS_Store
.tmp
tmp
dist
.env
**/package-lock.json
packages/**/.turbo
.turbo
*.tsbuildinfo

.env
.env.*
!.env.example


.generated

# Claude Code: share skills, keep local state out.
# A worktree under .claude/worktrees/ is a full nested checkout — its biome.jsonc
# makes `biome check .` fail with "Found a nested root configuration".
.claude/worktrees/
.claude/settings.local.json